# Server-side only. No NEXT_PUBLIC_ prefix means it never reaches the browser.
API_BASE_URL=http://localhost:8000

# Safe to expose: the anon key is designed for client use and is governed by RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SENTRY_DSN=
